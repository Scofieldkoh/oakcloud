name: A4 CORE C2 validation

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "C2 validation is paused while the deterministic correction is staged."
