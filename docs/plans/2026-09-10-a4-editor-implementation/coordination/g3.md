# G3 — fields/workflow independent verification

Status: **PASSED — automated independent acceptance complete**
Date: **2026-09-14**
Assignment: `VERIFY-Q2-G4-20260913-01`
Frozen contract: **v1 frozen at G0 — unchanged**

## Immutable product target

Independent verification tested the exact frozen Wave-4 product candidate:

`b522cf50f61f18b75f2cae62b42cd142bef4cf2d`

Integration PR: **#55 — A4Editor Wave 4: C3/S3/F3/W3 integration candidate**

The verifier branch and verifier-only workflow/test material were not product changes and are not required for promotion.

## Authoritative independent run

GitHub Actions run: `34822834846`

Environment:

- GitHub-hosted Ubuntu 24.04
- Node `24.20.0`
- npm `11.19.0`
- real Playwright Chromium / Chrome for Testing `149.0.7827.55`
- disposable PostgreSQL 16
- synthetic verification data only

Both decisive jobs asserted `git rev-parse HEAD` equals the immutable candidate before running product checks.

### Static, browser and actual output

Job `103908085379`: **SUCCESS**.

- pagination bundle regeneration / tracked-tree cleanliness: **PASS**
- TypeScript typecheck: **PASS**
- field grammar and authoring lifecycle: **6 files / 109 tests passed**
- workflow/output lifecycle: **4 files / 21 tests passed**
- prior-gate real Chromium regressions: **4 files / 68 tests passed**
- independent actual HTML/PDF verifier: **1 file / 1 test passed**

The actual-output verifier exercised production Chromium pagination and PDF generation with:

- boolean false: `BOOLEAN_FALSE:false`
- numeric zero: `NUMERIC_ZERO:0`
- date value: `DATE_VALUE:2026-09-14`
- multiline content
- semantic page break
- projection/runtime decoration removal from rendered output
- page header/footer
- multi-page content before and after the hard break

Direct PDF inspection with `pdfinfo` and `pdftotext` passed.

Generated PDF facts:

- pages: **3**
- page size: **A4 — 595.92 x 841.92 points**
- file size: **41,845 bytes**
- PDF version: **1.4**
- required first and post-break sentinels present in extracted text
- header/footer present on all three pages

Output evidence artifact:

- name: `a4-wave4-g3-output-evidence`
- artifact id: `10338767422`
- artifact size: `41,659` bytes
- digest: `sha256:f46a41d24bd4bab26c2c5731f18e28d60a01f38bad05d58850a318c1577ad2f7`
- source run: `34822834846`

The earlier actual-output failure in run `34819840908` was a verifier false positive: the sanitizer correctly removed `data-flow-id` from the rendered document body, while the verifier searched the entire HTML and matched the literal string inside the embedded pagination script. The correction changed only the verifier assertion scope; no product source changed.

### Disposable PostgreSQL revisions/concurrency

Job `103908085218`: **SUCCESS**.

- Prisma client generation: **PASS**
- migrations applied to disposable PostgreSQL: **PASS**
- revision, draft, compatibility and concurrency acceptance: **PASS**

## Gate decision

**G3 PASSED.**

The frozen Wave-4 candidate satisfies the automated independent G3 acceptance required before D2 behavior activation. No product defect was identified by this verification.

Under `verification-and-rollout.md`, G3 blocks D2, while G4/Q2 blocks D3/full programme completion. The user directed that human verification occur only after D2. Therefore:

1. Wave-4 product integration may be merged after explicit user authorization.
2. D2 is technically eligible after the merge, subject to separate rollout/deployment authorization and compatibility rules.
3. Human/browser-device/clipboard/IME/AT/staff-journey portions of G4/Q2 remain outstanding and are intentionally deferred until after D2.
4. D3 remains blocked until G4/Q2 is completed.

This gate record does **not** itself authorize deployment, production feature activation, strict rollout, real-document finalization, or an application version bump.
